# One config for the repo; each actors/<name>/ inherits it (ruff resolves upward).
line-length = 100
target-version = "py312"
exclude = [".venv", "storage", "apify_storage", "build"]

[lint]
select = ["E", "F", "I", "B", "UP"]
