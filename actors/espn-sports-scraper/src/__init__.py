"""`espn-sports-scraper` — scores, schedules, standings, teams and game summaries from the
unauthenticated JSON feeds espn.com uses (UTILS_SPEC §3). Same image serves
`tennis-scores-scraper` (§3.10)."""
