{
  "actorOutputSchemaVersion": 1,
  "title": "PDF text extractor output",
  "properties": {
    "items": {
      "type": "string",
      "title": "Extracted items",
      "description": "All page, chunk and document rows from this run as a JSON array.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?clean=true"
    }
  }
}
