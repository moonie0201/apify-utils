{
  "actorOutputSchemaVersion": 1,
  "title": "Tennis scores output",
  "properties": {
    "rows": {
      "type": "string",
      "title": "Rows",
      "description": "Every match row from this run as a JSON array, plus the free league_summary and error rows.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?clean=true"
    }
  }
}
